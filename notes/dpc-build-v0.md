# Ulatec DPC v0 Build

Source commit: 0868861

GitHub Actions run:

```text
28912824682
```

Release:

```text
dpc-v0
```

Local APK path on ANTIKYTHERA:

```text
artifacts/ulatec-dpc-debug-apk/app-debug.apk
```

Status:

- DPC source committed and pushed.
- GitHub Actions build succeeded.
- Debug APK downloaded to ANTIKYTHERA.
- Debug APK published as a GitHub release asset.
- APK is intentionally not committed to git.
- This is a test DPC, not the final no-exit kiosk.
